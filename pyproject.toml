[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "optcg-lab"
version = "0.2.0"
description = "Auditable local ONE PIECE Card Game deck and matchup lab"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
